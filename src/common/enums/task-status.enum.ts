/**
 * Reprsents the status of a task in the system.
 * Used to track the progress on the task.
 */
export enum TaskStatus{
    PENDING='PENDING',
    IN_PROGRESS='IN_PROGRESS',
    COMPLETED='COMPLETED'
}